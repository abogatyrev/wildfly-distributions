// in case of conflicts need to use: rpm -iv --replacefiles rpm_name.rpm
ospackage {
    packageName = 'apache-commons-lang3-wildfly-module'
    version = '3.4'
    release = '0'
    user = 'root'
    os = LINUX
    packager = 'Andrey Bogatyrev'
    buildHost = 'localhost'

    provides('apache-commons-lang3')
    provides('commons-lang3')
    provides('commons-lang3-wildfly')
    provides('commons-lang3-wildfly-module')

    requires('wildfly')

    // it's conflict with 'wildfly' installed rpm, if do copy to linked folder...
    into '/opt/wildfly/modules'

    from('/resources') {
        addParentDirs = false
        createDirectoryEntry = true
    }

    postInstall file('/scripts/postInstall.sh')
}