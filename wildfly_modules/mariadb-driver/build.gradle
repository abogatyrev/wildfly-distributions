ospackage {
    from('/resources/cli') {
        into '/tmp//mariadb-driver/cli'
    }

    postInstall file('resources/rpm/scripts/postInstall.sh')
    preUninstall file('resources/rpm/scripts/preUninstall.sh')
}